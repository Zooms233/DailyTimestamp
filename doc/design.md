# 时间戳记录工具 · MVP 前端设计

> 用途：生活中不间断地为每个事件打时间戳，回顾时了解自己的时间分配。
> 本设计为最小可行性（MVP）方案：**一行代码不写多余功能**，先跑通"打卡 → 记录 → 回看"闭环。

---

## 1. MVP 范围

### 做（本轮实现）
| 能力 | 说明 |
|---|---|
| 打卡 | 一键标记"现在开始做什么"，自动结束上一个事件 → 时间线**不间断** |
| 今日时间轴 | 按时间顺序列出今日事件及每段时长，进行中的事件高亮 |
| 按日统计 | 每天各标签累计时长 + 占比条形图，可翻"上一日/下一日"回看历史 |
| 标签 | 分类管理页：默认 12 个，可添加/删除/排序（首页右上角 ⚙ 进入） |
| 备注 | 打卡时可选填，随事件存储，时间轴上小字展示 |
| 本地存储 | 事件数据落盘为 JSON 文件，重启不丢 |

### 不做（留到 V2）
- 完整事件编辑（时间/备注/删除）、撤销——已支持修改类别、与上一个合并（见 3.1）
- 周/月/年统计、日历视图、图表库
- 提醒、通知、云同步、多设备

---

## 2. 信息架构

```
App
├─ Tab1 打卡页（HomePage）   ← 默认页，90% 时间停留在这
│    ├─ 进行中事件卡片（固定底部）
│    ├─ 大按钮「记录新事件」 → 弹出 PunchSheet
│    ├─ 今日时间轴列表（主视区，可滚动）
│    └─ 右上角 ⚙ → 分类管理页（LabelsPage，独立路由）
│         ├─ 添加 / 删除分类
│         └─ [↑]/[↓] 顺序调整（即打卡弹窗显示顺序）
└─ Tab2 统计页（StatsPage）
     ├─ 当日按标签时长 + 占比条形
     ├─ 未记录时段（空档）提示
     └─ 上一日 / 今日 / 下一日 翻页
```

技术要点：单页双 Tab，无路由；分类管理页用 `Navigator.push` 独立路由；`setState` + 内存中事件列表，无需状态管理库。

---

## 3. 界面字符画

### 3.1 打卡页（默认首页）

```text
```text
┌──────────────────────────────────────────────┐
│ 时间戳记录                         [统计]    │
├──────────────────────────────────────────────┤
│                                              │
│ 今日时间轴（5 个事件）                       │
│                                              │
│  07:00-07:30  晨跑              30 分        │
│  07:30-07:55  早餐              25 分        │
│  07:55-08:32  通勤              37 分        │
│  08:32-现在   阅读《原子习惯》   进行中 ▶    │
│                                              │
│ ● 进行中：阅读《原子习惯》                   │
│    开始于 08:32，已持续 1 小时 24 分         │
│                                              │
│        ┌────────────────────────────┐        │
│        │        ＋ 记录新事件       │        │
│        └────────────────────────────┘        │
│                                              │
│ [首页]                  [统计]               │
└──────────────────────────────────────────────┘
```
```




要点：
- 布局调整（2026-03-05）：时间轴占主视区可滚动；进行中卡片 + 大按钮**固定底部**（拇指区），不随列表滚走，单手操作更顺。
- 进行中卡片：唯一"进行中"事件，展示开始时间与已持续时长（每秒刷新）。
- 大按钮贴底，是全屏最易点元素。
- 时间轴行 = `开始-结束  标签  时长`，进行中行带 `▶` 标记。
- **点击任意事件行** → 弹出操作：① 修改类别（选择后即时生效）；② 与上一个合并（**二次确认**：明示双方类别/时长与合并后归属；合并后保留上一事件类别，备注合并，本事件删除）；③ 拆分事件（**双滚轮：时/分**，小时列表按事件范围限界；分钟滚轮启用循环，滚过 00/59 自动进位小时 ±1，事件边界处弹回停住；末小时分钟收敛余量；大字实时显示；按钮含时刻确认）——均为防误触/修正手段。
- 每次打卡时自动把上一条 `结束=现在`，行间无缝隙（规则不变）。

### 3.2 打卡弹窗（点击大按钮弹出 BottomSheet）

```text
┌──────────────────────────────────────────────┐
│  现在在做什么？                              │
├──────────────────────────────────────────────┤
│  [睡眠] [清洁] [吃饭] [行路] [学习] [运动]   │
│  [代码] [阅读] [游戏] [碎片信息流]           │
│  [总结] [其他]                               │
│                                              │
│  ┌────────────────────────────────────────┐  │
│  │  备注（可空，例如：《原子习惯》）      │  │
│  └────────────────────────────────────────┘  │
│                                              │
│ （点上方任一标签，立即开始记录）             │
└──────────────────────────────────────────────┘
```



要点：
- 点任一标签 **立即** 记录并关闭（少一次点击）；同时提交备注框内容（可空）。
- 备注（可空）：随事件存储，在时间轴/进行中卡片以**小字**（sub 风格）跟在标签后展示，如 `阅读《原子习惯》`；不参与统计聚合。
- 已移除：自定义新标签输入框（改为 3.4 分类管理页）、底部兜底按钮（误触关闭弹窗即不改变任何状态）。
- MVP 不提供"取消打卡"按钮：已在进行的旧事件会随新打卡自动结束，这是设计意图；若想继续旧事件，关掉弹窗即可（什么都不发生）。

### 3.3 统计页

```text
```text
┌──────────────────────────────────────────────┐
│ 时间分配                2026-03-03 周二      │
├──────────────────────────────────────────────┤
│ 已记录 9h20m · 未记录 2h10m                  │
│                                              │
│ 阅读   ████████████░░░░░░  2h40m  29%        │
│ 工作   ██████████░░░░░░░░  2h05m  22%        │
│ 睡眠   ████████░░░░░░░░░░  1h50m  20%        │
│ 通勤   ███░░░░░░░░░░░░░░░  0h45m   8%        │
│ 餐饮   ██░░░░░░░░░░░░░░░░  0h30m   5%        │
│ 空档   ░░░░░░░░░░░░░░░░░░  1h30m  16%        │
│                                              │
│  [上一日]        [今日]        [下一日]      │
│                                              │
│ [首页]                  [统计]               │
└──────────────────────────────────────────────┘
```
```



要点：
- 按标签聚合当日时长，条形图长度 = 占比（纯文本字符，不引入图表库）。
- **空档** = 日首（0 点～首个事件）与日尾（最后事件结束～24 点）的未记录时间，让"没记录"也透明可见。
- 上一日/下一日翻页回看任意一天（响应"回顾"核心诉求的最小实现）。今日按钮一键回今日。
- 无记录的一天显示"这一天没有记录"。

### 3.4 分类管理页（首页右上角 ⚙ 进入）

```text
┌──────────────────────────────────────────────┐
│  分类管理                                    │
├──────────────────────────────────────────────┤
│  ┌────────────────────────────────────────┐  │
│  │  新分类名称…                  [添加]  │   │
│  └────────────────────────────────────────┘  │
│                                              │
│  睡眠                        [↑] [↓] [🗑]     │
│  清洁                        [↑] [↓] [🗑]     │
│  吃饭                        [↑] [↓] [🗑]     │
│  行路                        [↑] [↓] [🗑]     │
│  ...                                         │
│                                              │
│  提示：删除分类仅从打卡列表移除，历史事件保留│
└──────────────────────────────────────────────┘
```


要点：
- 添加：顶部输入框 + [添加]（回车亦可）；重名/空名拒绝。
- 排序：每行 [↑]/[↓] 调整顺序，即打卡弹窗标签的显示顺序。
- **更换颜色**：点分类行首色块 → 主色板弹窗 → 点选即生效（持久化到 `colors` 字段）；默认色按分类语义自动指派（睡眠=indigo、吃饭=orange…），自定义分类取色板未占用色，稳定不串色。
- 删除：仅从打卡列表移除；历史事件与统计保留（事件按 label 字符串聚合，不受影响）。
- 默认分类 12 个（睡眠/清洁/吃饭/行路/学习/运动/代码/阅读/游戏/碎片信息流/总结/其他），存于 events.json 顶层 `labels` 字段（有序数组）。

---

## 4. 核心交互规则（不间断时间戳）

```
[点击 记录新事件] ──► 弹窗选择标签
                        │
                        ▼
     存在进行中事件？ ──否──► 新事件 startAt = 现在
        │是
        ▼
   旧事件 endAt = 现在 ──► 新事件 startAt = 现在
```

- 规则 1：**任意时刻至多一个进行中事件**。
- 规则 2：新事件开始 = 旧事件结束，时间线天然连续、无重叠、无缝隙。
- 规则 3：事件结束后可**修改类别**、可**与上一个合并**（防误触修正）；不可修改起止时间、备注，不可单独删除（V2）。
- 规则 4：App 冷启动时恢复"进行中"状态（进行中事件标为 endAt=null，可跨天持续）。

---

## 5. 数据模型

```json
{
  "labels": ["睡眠", "清洁", "吃饭", "行路", "学习", "运动", "代码", "阅读", "游戏", "碎片信息流", "总结", "其他"],  // 有序分类，顺序=打卡弹窗显示顺序
  "colors": { "碎片信息流": -10720169 },  // 用户改过的分类色（ARGB int）；缺失时按语义表+调色板确定性指派
  "events": [
    {
      "id": "1719983520000_ab12",      // 毫秒时间戳 + 随机后缀
      "label": "阅读",
      "startAt": 1719983520000,        // 开始，毫秒 epoch
      "endAt": 1719985920000,          // 结束；null = 进行中
      "note": "《原子习惯》第 3 章"     // 备注；可空（null）
    }
  ]
}
```

- 纯 Dart 类 `TimestampEvent`，手写 toJson/fromJson，零依赖。
- 时区一律用本地时间；跨天事件按"结束日"归属统计，进行中事件在统计中截止到"现在"。
- `labels` 缺失时（旧数据）回退为默认 12 分类；删除分类不影响历史事件（事件里仍存 label 字符串）与统计。

## 6. 存储与技术选型

| 项 | 选择 | 理由 |
|---|---|---|
| 存储 | 应用文档目录单文件 `events.json`，整文件读写 | MVP 数据量小（一天几十条），无数据库复杂度；单文件天然保证 `events` 与 `customLabels` 一致，备份/迁移=拷一个文件 |
| 写盘 | **原子写**：先写 `events.json.tmp`，成功后 rename 覆盖 | 防断电/崩溃导致 JSON 半写损坏（两行代码成本，MVP 标配） |
| 依赖 | 仅 `path_provider`（官方） | 获取应用目录的最低成本；其余全部 Flutter SDK |
| 状态 | `setState` + 单例 `EventStore` | 事件流单一，状态简单 |
| 平台 | Android + Windows | 手机随身打点；Windows 本机调试回看。Web 需要 localStorage 存储适配且与桌面调试重复，V2 再议 |
| 字体 | 思源黑体（直接指定系统字体名，不打包） | 打包自定义字体与 MaterialIcons 在 Windows 渲染冲突（霞鹜文楷、思源黑体两次复现图标方块），改用 `fontFamily: 'Source Han Sans SC'` 直接指定：Windows 本机已装命中；Android 无此字体名时回退默认中文（Noto Sans CJK，与思源同源），效果一致 |
| 时间格式化 | 手写 `HH:mm` / `x 小时 y 分` 工具函数 | 不用 intl，一个函数 10 行 |

## 7. 代码结构

```
app/lib/
├── main.dart                  # 入口 + 双 Tab 骨架
├── models/timestamp_event.dart# 数据模型 + 序列化
├── services/event_store.dart  # 加载/保存/打卡/统计聚合
├── pages/home_page.dart       # 打卡页（进行中卡片+时间轴+右上角分类管理入口）
├── pages/labels_page.dart     # 分类管理页（添加/删除/上移下移）
├── pages/stats_page.dart      # 统计页（按日聚合+翻页）
└── widgets/punch_sheet.dart   # 打卡弹窗（标签选择）
```

## 8. 验收用例（人工核验）

1. 首次打开 → 无进行中事件，时间轴为空，出现"记录新事件"大按钮。
2. 点大按钮 → 弹窗出现；在备注框填《原子习惯》后点「阅读」→ 弹窗关闭，卡片显示"进行中：阅读《原子习惯》"（备注小字），时间轴出现 `现在-现在 阅读 进行中 ▶`。
3. 隔几分钟点「工作」（备注留空）→ 阅读行变为 `开始-结束` 且时长正确，无备注不再显示小字；两条时间无缝衔接。
4. 杀进程重启 → 进行中事件仍为进行中，历史数据完整。
5. 切到统计页 → 今日百分比合计 = 100%（含空档），条形长度与占比一致。
6. 点上一日/下一日翻页 → 无记录日显示"这一天没有记录"。
7. 首页右上角 ⚙ → 分类管理：添加"冥想" → 弹窗出现该标签；上移/下移顺序在弹窗同步变化；删除"冥想" → 弹窗消失该标签，但历史记录与统计仍含"冥想"。
8. 分类管理重启后顺序、增删结果保持。
9. 各分类有独立颜色：打卡弹窗 chip、时间轴行左侧色条、进行中圆点、统计条形同色一致；分类管理页点色块换色后以上各处即时同步。
10. 点击今日时间轴某行（如误触产生的「其他」）→ 选「修改类别」→ 改为正确类别，统计随之变化。
11. 点击事件行 → 选「与上一个合并」→ **弹出确认框**（显示双方类别与时长、说明合并后保留上一事件类别）→ 取消则无变化；确认后两条变一条（时长相加），备注合并；第一条事件合并时提示"已是第一个事件，无法合并"。
12. 点击事件行 → 选「拆分事件」→ 底部弹窗：**时/分双滚轮**（小时列表 0..maxH 为事件范围上限；末小时分钟受剩余分钟余量限制，超限自动收敛）+ 大字实时时刻 +「在 10:34 拆分」按钮 → 点按钮即拆为两段（同类别同备注）；进行中事件拆分后第二段仍为进行中；事件短于 2 分钟提示无法拆分。

---

## 9. V2 展望（本轮不实现）

- 事件编辑/删除、撤销
- 周/月报告、日历热力图
- 标签批量导入/导出、长按拖拽排序
- 云同步 / 导出 CSV